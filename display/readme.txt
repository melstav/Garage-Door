The NodeMCU firmware I used for this project was a custom build from
https://nodemcu-build.com/ with the following modules enabled:
enduser_setup, file, gpio, mqtt, net, node, tmr, uart, wifi.

Not all of these modules were used by the lua script I wrote.
